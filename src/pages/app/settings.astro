---
import Layout from "../../layouts/Layout.astro";
---

<Layout title="Settings // PostPulsar">
  <main class="container py-12">
    <div class="mb-12">
      <h1 class="text-3xl font-bold uppercase">
        // System Preferences & Security
      </h1>
      <p class="mt-1 text-foreground/70">
        Manage your account details and security protocols.
      </p>
    </div>

    <div class="space-y-8">
      <!-- Email Management -->
      <div class="border border-border p-6">
        <h2 class="border-b border-border pb-3 text-xl font-bold uppercase">
          Email Address
        </h2>
        <div class="mt-4 flex items-center justify-between">
          <div>
            <p class="font-mono text-sm uppercase text-foreground/70">
              Current Email Address:
            </p>
            <p id="user-email" class="font-mono text-lg">Loading...</p>
          </div>
          <button
            id="change-email-btn"
            class="border border-border px-4 py-2 font-mono text-sm font-bold uppercase text-foreground transition-colors hover:bg-border/50"
          >
            Change
          </button>
        </div>
        <div id="email-message" class="mt-4 text-sm"></div>
      </div>

      <!-- Password Management -->
      <div class="border border-border p-6">
        <h2 class="border-b border-border pb-3 text-xl font-bold uppercase">
          Password
        </h2>

        <div id="password-loading" class="mt-4">
          <p class="font-mono text-sm text-foreground/70">
            // Verifying authentication method...
          </p>
        </div>

        <!-- Form for users WITH an existing password -->
        <div id="password-change-wrapper" class="hidden">
          <p class="mt-4 font-mono text-sm text-foreground/70">
            You have a password set. You can change it below.
          </p>
          <form id="password-change-form" class="mt-4 space-y-4">
            <div>
              <label
                for="current-password"
                class="mb-2 block font-mono text-xs font-bold uppercase tracking-widest text-foreground/70"
                >Current Password</label
              >
              <input
                type="password"
                id="current-password"
                name="current-password"
                class="w-full rounded-none border border-border bg-background p-3 font-mono focus:border-primary focus:outline-none focus:ring-0"
                required
              />
            </div>
            <div>
              <label
                for="new-password-change"
                class="mb-2 block font-mono text-xs font-bold uppercase tracking-widest text-foreground/70"
                >New Password</label
              >
              <input
                type="password"
                id="new-password-change"
                name="new-password"
                class="w-full rounded-none border border-border bg-background p-3 font-mono focus:border-primary focus:outline-none focus:ring-0"
                required
              />
            </div>
            <div>
              <label
                for="confirm-password-change"
                class="mb-2 block font-mono text-xs font-bold uppercase tracking-widest text-foreground/70"
                >Confirm New Password</label
              >
              <input
                type="password"
                id="confirm-password-change"
                name="confirm-password"
                class="w-full rounded-none border border-border bg-background p-3 font-mono focus:border-primary focus:outline-none focus:ring-0"
                required
              />
            </div>
            <button
              type="submit"
              class="w-full border border-primary bg-primary p-3 font-mono text-sm font-bold uppercase text-background transition-colors hover:bg-primary/80"
            >
              Update Password
            </button>
          </form>
        </div>

        <!-- Form for users WITHOUT a password (social login) -->
        <div id="password-create-wrapper" class="hidden">
          <p class="mt-4 font-mono text-sm text-foreground/70">
            You signed in using a social provider. Create a password to enable
            signing in with your email as well.
          </p>
          <form id="password-create-form" class="mt-4 space-y-4">
            <div>
              <label
                for="new-password-create"
                class="mb-2 block font-mono text-xs font-bold uppercase tracking-widest text-foreground/70"
                >New Password</label
              >
              <input
                type="password"
                id="new-password-create"
                name="new-password"
                class="w-full rounded-none border border-border bg-background p-3 font-mono focus:border-primary focus:outline-none focus:ring-0"
                required
              />
            </div>
            <div>
              <label
                for="confirm-password-create"
                class="mb-2 block font-mono text-xs font-bold uppercase tracking-widest text-foreground/70"
                >Confirm New Password</label
              >
              <input
                type="password"
                id="confirm-password-create"
                name="confirm-password"
                class="w-full rounded-none border border-border bg-background p-3 font-mono focus:border-primary focus:outline-none focus:ring-0"
                required
              />
            </div>
            <button
              type="submit"
              class="w-full border border-primary bg-primary p-3 font-mono text-sm font-bold uppercase text-background transition-colors hover:bg-primary/80"
            >
              Create Password
            </button>
          </form>
        </div>

        <div id="password-message" class="mt-4 text-sm"></div>
      </div>

      <!-- Social Connections -->
      <div class="border border-border p-6">
        <h2 class="border-b border-border pb-3 text-xl font-bold uppercase">
          Social Connections
        </h2>
        <div class="mt-4">
          <button
            id="link-google-btn"
            class="flex w-full items-center justify-center border border-border bg-transparent p-3 font-mono text-sm font-bold uppercase text-foreground transition-colors hover:bg-border/50"
          >
            <svg class="mr-3 size-5" viewBox="0 0 48 48">
              <path
                fill="#FFC107"
                d="M43.611 20.083H42V20H24v8h11.303c-1.649 4.657-6.08 8-11.303 8c-6.627 0-12-5.373-12-12s5.373-12 12-12c3.059 0 5.842 1.154 7.961 3.039l5.657-5.657C34.046 6.053 29.268 4 24 4C12.955 4 4 12.955 4 24s8.955 20 20 20s20-8.955 20-20c0-1.341-.138-2.65-.389-3.917z"
              ></path><path
                fill="#FF3D00"
                d="M6.306 14.691l6.571 4.819C14.655 15.108 18.961 12 24 12c3.059 0 5.842 1.154 7.961 3.039l5.657-5.657C34.046 6.053 29.268 4 24 4C16.318 4 9.656 8.337 6.306 14.691z"
              ></path><path
                fill="#4CAF50"
                d="M24 44c5.166 0 9.86-1.977 13.409-5.192l-6.19-5.238A11.91 11.91 0 0 1 24 36c-5.202 0-9.619-3.317-11.283-7.946l-6.522 5.025C9.505 39.556 16.227 44 24 44z"
              ></path><path
                fill="#1976D2"
                d="M43.611 20.083H42V20H24v8h11.303c-.792 2.237-2.231 4.166-4.087 5.571l6.19 5.238C42.018 36.223 44 30.552 44 24c0-1.341-.138-2.65-.389-3.917z"
              ></path>
            </svg>
            <span id="link-google-btn-text">Link Google Account</span>
          </button>
        </div>
      </div>

      <!-- Danger Zone -->
      <div class="border-2 border-red-500/50 p-6">
        <h2 class="border-b border-red-500/30 pb-3 text-xl font-bold uppercase text-red-500">
          Danger Zone
        </h2>
        <div class="mt-4">
          <p class="mb-4 text-foreground/70">
            Deleting your account is a permanent action and cannot be undone.
          </p>
          <button
            id="delete-account-btn"
            class="w-full border border-red-500 p-3 font-mono text-sm font-bold uppercase text-red-500 transition-colors hover:bg-red-500/10"
          >
            Delete My Account
          </button>
        </div>
      </div>
    </div>
  </main>

  <!-- Email Change Modal -->
  <div
    id="email-modal"
    class="fixed inset-0 z-50 hidden size-full items-center justify-center bg-background/80 backdrop-blur-sm"
  >
    <div class="w-full max-w-md border border-border bg-background p-8">
      <h3 class="mb-6 text-center text-xl font-bold uppercase">
        // Initiate Email Change
      </h3>
      <div class="space-y-4">
        <input
          type="email"
          id="new-email-input"
          placeholder="Enter your new email"
          class="w-full rounded-none border border-border bg-background p-3 font-mono focus:border-primary focus:outline-none focus:ring-0"
        />
        <input
          type="password"
          id="password-for-email-change"
          placeholder="Enter your current password to confirm"
          class="w-full rounded-none border border-border bg-background p-3 font-mono focus:border-primary focus:outline-none focus:ring-0"
          required
        />
        <button
          id="confirm-email-change-btn"
          class="w-full border border-primary bg-primary p-3 font-mono text-sm font-bold uppercase text-background transition-colors hover:bg-primary/80"
        >
          Confirm Change
        </button>
        <button
          id="cancel-email-change-btn"
          class="w-full border border-border bg-transparent p-3 font-mono text-sm font-bold uppercase text-foreground transition-colors hover:bg-border/50"
        >
          Cancel
        </button>
      </div>
      <div id="modal-email-message" class="mt-4 text-sm"></div>
    </div>
  </div>

  <!-- Delete Account Modal -->
  <div
    id="confirm-delete-modal"
    class="fixed inset-0 z-50 hidden size-full items-center justify-center bg-background/80 backdrop-blur-sm"
  >
    <div class="w-full max-w-md border border-red-500 bg-background p-8">
      <h3 class="mb-4 text-center text-xl font-bold uppercase text-red-500">
        // Confirm Termination
      </h3>
      <p class="mb-6 text-center text-foreground/70">
        This action is irreversible. Are you sure you want to permanently delete
        your account and all associated data?
      </p>
      <div class="flex gap-4">
        <button
          id="cancel-delete-btn"
          class="w-full border border-border bg-transparent p-3 font-mono text-sm font-bold uppercase text-foreground transition-colors hover:bg-border/50"
        >
          Cancel
        </button>
        <button
          id="confirm-delete-btn"
          class="w-full border border-red-500 bg-red-500 p-3 font-mono text-sm font-bold uppercase text-white transition-colors hover:bg-red-500/80"
        >
          Confirm Deletion
        </button>
      </div>
    </div>
  </div>
</Layout>

<script>
  import { supabase } from "../../lib/supabase";

  async function setupAccountPage() {
    const { data, error } = await supabase.auth.getSession();
    if (error || !data.session) {
      window.location.href = "/login";
      return;
    }

    const user = data.session.user;
    const userEmailEl = document.getElementById("user-email");
    if (userEmailEl && user.email) {
      userEmailEl.textContent = user.email;
    }

    // --- Password Management ---
    const passwordLoadingEl = document.getElementById("password-loading");
    const passwordChangeWrapper = document.getElementById(
      "password-change-wrapper",
    );
    const passwordCreateWrapper = document.getElementById(
      "password-create-wrapper",
    );
    const passwordMessageEl = document.getElementById("password-message");

    if (
      passwordLoadingEl &&
      passwordChangeWrapper &&
      passwordCreateWrapper &&
      passwordMessageEl
    ) {
      // This is the robust way to check if a user has a password identity.
      const hasPasswordIdentity =
        user.identities &&
        user.identities.some((identity) => identity.provider === "email");

      passwordLoadingEl.classList.add("hidden");

      if (hasPasswordIdentity) {
        // Show the CHANGE password form
        passwordChangeWrapper.classList.remove("hidden");
        const passwordChangeForm = document.getElementById("password-change-form");

        if (passwordChangeForm) {
          passwordChangeForm.addEventListener("submit", async (e) => {
            e.preventDefault();
            const form = e.target as HTMLFormElement;
            passwordMessageEl.textContent = "";

            const currentPassword = (form.elements.namedItem(
              "current-password",
            ) as HTMLInputElement).value;
            const newPassword = (form.elements.namedItem(
              "new-password-change",
            ) as HTMLInputElement).value;
            const confirmPassword = (form.elements.namedItem(
              "confirm-password-change",
            ) as HTMLInputElement).value;

            if (!currentPassword || !newPassword || !confirmPassword) {
              passwordMessageEl.textContent = "[ERROR] :: All fields are required.";
              passwordMessageEl.className = "font-mono text-red-500";
              return;
            }
            if (newPassword.length < 6) {
              passwordMessageEl.textContent =
                "[ERROR] :: New password must be at least 6 characters.";
              passwordMessageEl.className = "font-mono text-red-500";
              return;
            }
            if (newPassword !== confirmPassword) {
              passwordMessageEl.textContent = "[ERROR] :: New passwords do not match.";
              passwordMessageEl.className = "font-mono text-red-500";
              return;
            }

            const { error: signInError } = await supabase.auth.signInWithPassword({
              email: user.email!,
              password: currentPassword,
            });

            if (signInError) {
              passwordMessageEl.textContent =
                "[ERROR] :: Your current password is incorrect.";
              passwordMessageEl.className = "font-mono text-red-500";
              return;
            }

            const { error: updateError } = await supabase.auth.updateUser({
              password: newPassword,
            });

            if (updateError) {
              passwordMessageEl.textContent = `[ERROR] :: ${updateError.message}`;
              passwordMessageEl.className = "font-mono text-red-500";
            } else {
              passwordMessageEl.textContent = "[SUCCESS] :: Password updated.";
              passwordMessageEl.className = "font-mono text-green-500";
              form.reset();
            }
          });
        }
      } else {
        // Show the CREATE password form
        passwordCreateWrapper.classList.remove("hidden");
        const passwordCreateForm = document.getElementById("password-create-form");

        if (passwordCreateForm) {
          passwordCreateForm.addEventListener("submit", async (e) => {
            e.preventDefault();
            const form = e.target as HTMLFormElement;
            passwordMessageEl.textContent = "";

            const newPassword = (form.elements.namedItem(
              "new-password-create",
            ) as HTMLInputElement).value;
            const confirmPassword = (form.elements.namedItem(
              "confirm-password-create",
            ) as HTMLInputElement).value;

            if (newPassword.length < 6) {
              passwordMessageEl.textContent =
                "[ERROR] :: Password must be at least 6 characters.";
              passwordMessageEl.className = "font-mono text-red-500";
              return;
            }
            if (newPassword !== confirmPassword) {
              passwordMessageEl.textContent = "[ERROR] :: Passwords do not match.";
              passwordMessageEl.className = "font-mono text-red-500";
              return;
            }

            const { error: updateError } = await supabase.auth.updateUser({
              password: newPassword,
            });

            if (updateError) {
              passwordMessageEl.textContent = `[ERROR] :: ${updateError.message}`;
              passwordMessageEl.className = "font-mono text-red-500";
            } else {
              passwordMessageEl.textContent =
                "[SUCCESS] :: Password created. You can now sign in with it.";
              passwordMessageEl.className = "font-mono text-green-500";
              // We can reload the page to show the correct form now
              setTimeout(() => window.location.reload(), 2000);
            }
          });
        }
      }
    }

    // --- Email Management ---
    const emailModal = document.getElementById("email-modal");
    const changeEmailBtn = document.getElementById("change-email-btn");
    const confirmEmailChangeBtn = document.getElementById(
      "confirm-email-change-btn",
    );
    const cancelEmailChangeBtn = document.getElementById(
      "cancel-email-change-btn",
    );
    const newEmailInput = document.getElementById(
      "new-email-input",
    ) as HTMLInputElement;
    const passwordForEmailChangeInput = document.getElementById(
      "password-for-email-change",
    ) as HTMLInputElement;
    const emailMessageEl = document.getElementById("email-message");
    const modalEmailMessageEl = document.getElementById("modal-email-message");

    if (
      emailModal &&
      changeEmailBtn &&
      confirmEmailChangeBtn &&
      cancelEmailChangeBtn &&
      newEmailInput &&
      passwordForEmailChangeInput &&
      emailMessageEl &&
      modalEmailMessageEl
    ) {
      changeEmailBtn.addEventListener("click", () => {
        emailMessageEl.textContent = "";
        modalEmailMessageEl.textContent = "";
        newEmailInput.value = "";
        passwordForEmailChangeInput.value = "";
        emailModal.classList.remove("hidden");
        emailModal.classList.add("flex");
      });

      cancelEmailChangeBtn.addEventListener("click", () => {
        emailModal.classList.add("hidden");
        emailModal.classList.remove("flex");
      });

      confirmEmailChangeBtn.addEventListener("click", async () => {
        const newEmail = newEmailInput.value;
        const password = passwordForEmailChangeInput.value;
        modalEmailMessageEl.textContent = "";

        if (!newEmail || !password) {
          modalEmailMessageEl.textContent = "[ERROR] :: Both fields are required.";
          modalEmailMessageEl.className = "font-mono text-red-500";
          return;
        }

        // 1. Verify password
        const { error: signInError } = await supabase.auth.signInWithPassword({
          email: user.email!,
          password: password,
        });

        if (signInError) {
          modalEmailMessageEl.textContent = "[ERROR] :: Your password is incorrect.";
          modalEmailMessageEl.className = "font-mono text-red-500";
          return;
        }

        // 2. If password is correct, update email
        if (newEmail !== user.email) {
          const { error: updateError } = await supabase.auth.updateUser({
            email: newEmail,
          });

          if (updateError) {
            // Show error in the main page message area, not the modal
            emailMessageEl.textContent = `[ERROR] :: ${updateError.message}`;
            emailMessageEl.className = "font-mono text-red-500";
          } else {
            emailMessageEl.innerHTML = `
              <div class="border border-yellow-500/50 bg-yellow-500/10 p-4 font-mono text-sm text-yellow-300">
                <h4 class="font-bold uppercase">// Action Required</h4>
                <p class="mt-2">To complete the change, you must click the confirmation link sent to BOTH your <strong>OLD and NEW</strong> email addresses.</p>
              </div>
            `;
          }
          emailModal.classList.add("hidden");
          emailModal.classList.remove("flex");
        } else {
          modalEmailMessageEl.textContent =
            "[WARNING] :: Please enter a different email address.";
          modalEmailMessageEl.className = "font-mono text-yellow-500";
        }
      });
    }

    // --- Google Account Linking ---
    const linkGoogleBtn = document.getElementById("link-google-btn");
    const linkGoogleBtnText = document.getElementById("link-google-btn-text");

    if (user && linkGoogleBtn && linkGoogleBtnText) {
      const isGoogleLinked = user.app_metadata.providers.includes("google");
      if (isGoogleLinked) {
        linkGoogleBtnText.textContent = "Google Account Linked";
        (linkGoogleBtn as HTMLButtonElement).disabled = true;
        linkGoogleBtn.classList.add(
          "border-green-500",
          "text-green-500",
          "cursor-not-allowed",
        );
      } else {
        linkGoogleBtn.addEventListener("click", async () => {
          const { error } = await supabase.auth.signInWithOAuth({
            provider: "google",
            options: {
              redirectTo: window.location.href,
            },
          });
          if (error) {
            alert(`[ERROR] :: Could not link Google account: ${error.message}`);
          }
        });
      }
    }

    // --- Delete Account ---
    const deleteAccountBtn = document.getElementById("delete-account-btn");
    const confirmDeleteModal = document.getElementById("confirm-delete-modal");
    const cancelDeleteBtn = document.getElementById("cancel-delete-btn");
    const confirmDeleteBtn = document.getElementById("confirm-delete-btn");

    if (deleteAccountBtn && confirmDeleteModal && cancelDeleteBtn && confirmDeleteBtn) {
      deleteAccountBtn.addEventListener("click", () => {
        confirmDeleteModal.classList.remove("hidden");
        confirmDeleteModal.classList.add("flex");
      });

      cancelDeleteBtn.addEventListener("click", () => {
        confirmDeleteModal.classList.add("hidden");
        confirmDeleteModal.classList.remove("flex");
      });

      confirmDeleteBtn.addEventListener("click", async () => {
        const { error } = await supabase.functions.invoke("delete-user", {
          method: "POST",
        });

        if (error) {
          alert(`[ERROR] :: Could not delete account: ${error.message}`);
        } else {
          alert("[SUCCESS] :: Account terminated.");
          await supabase.auth.signOut();
          window.location.href = "/";
        }
      });
    }
  }

  setupAccountPage();
</script>
